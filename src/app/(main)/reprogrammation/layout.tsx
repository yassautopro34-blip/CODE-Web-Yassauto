import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Reprogrammation moteur légale à Gigean près de Montpellier",
  description:
    "Reprogrammation moteur Stage 1 et conversion E85 selon le véhicule, avec diagnostic et étude personnalisée à Gigean.",
};

export default function ReprogrammationLayout({ children }: { children: React.ReactNode }) {
  return children;
}
